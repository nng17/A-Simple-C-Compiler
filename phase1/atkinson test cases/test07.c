struct foo *foo(void) 
{
    long x, z;
    const int y;
    x = y * z - x / y + z % y;
    if (x == y || z == 3 && x != y) return (struct foo *) x;
}


int main(void)
{
    int x, y, z;
    unsigned *_a1_2_A_B_[10];

    switch(0) {
	case 10:
	default:
	    break;
    }

    do {
	x++ - --z;
	continue;
    } while (1 || 0);
}
